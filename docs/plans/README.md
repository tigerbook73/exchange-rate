# 阶段索引

记录每个迭代阶段的状态，一行一条。阶段完成并归档后，详细计划文档（`phase-N-*.md`）正文可以删除，只保留这里的摘要。

## 路线图（可随阶段推进调整）

| #   | 阶段                                             | 状态   | 完成日期   | 一句话结论                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------ | ------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | 项目脚手架初始化                                 | 已完成 | 2026-07-24 | Next.js 16(App Router)+TS+Tailwind+shadcn/ui(Base UI)+next-themes 打通，lint/typecheck/test/build 四条流水线通过，业务依赖装齐，详见 AGENTS.md「目录结构约定」「技术栈约束」                                                                                                                                                                                       |
| 2   | `/api/today` + `/api/history` 实现               | 已完成 | 2026-07-24 | 用真实源页面核实了选择器与"周末发布节奏"（周六有报价、只有周日没有，纠正了此前错误假设），实现 cheerio 解析 + 时区/跨年处理 + Route Handler，10 条单测全过，真实网络验证两个端点返回正确                                                                                                                                                                           |
| 3   | IndexedDB 封装模块                               | 已完成 | 2026-07-24 | `idb` 封装 `fx-cache/rates`（`rates-db.ts`），28 天窗口内空档用 carried-forward 填补 + 自动清除标记（`rates-sync.ts`），18 条单测全过（含 fake-indexeddb 端到端场景）                                                                                                                                                                                              |
| 4   | 首页 UI（卡片 + 图表 + 时间范围切换）            | 已完成 | 2026-07-24 | 卡片+图表+7/30/全部切换+下拉刷新+主题联动全部接入；carried-forward 用空心点+提示呈现，不可恢复空档用断线（null 值）呈现；4 条 e2e（mock API）+ 6 条单测覆盖；过程中修了一个真实 bug：图表最后一天的点因为没给 layout padding 被裁掉了                                                                                                                              |
| 5   | PWA 接入（@serwist/next + manifest）             | 已完成 | 2026-07-24 | `app/manifest.ts` + 生成的图标、`sw.ts` 复用 `defaultCache`、亮暗 `theme-color` via `viewport`；踩坑并解决了 `@serwist/next`(webpack) 与 Turbopack 的冲突（dev 禁用+`build --webpack`）；Playwright 实测 manifest/SW 注册/离线重载全部工作正常                                                                                                                     |
| 6   | 本地全流程测试 + 人工评审 + 部署 Vercel 真机验证 | 已完成 | 2026-07-24 | 全量回归通过；375-430px 视口无横向滚动；PWA 可安装性条件全部满足。**人工评审后的改动**：去掉主题手动切换按钮（只跟随系统）；`RateCard` 改成结构稳定的加载态（同步中显示上一次的值+行内"加载中"提示，不整体替换成占位文字）。已部署到 Vercel，真机验证通过；后续迭代（增量拉取历史数据、缓存窗口拆分、更多时间范围选项等）不再走独立 phase 文档，直接以功能提交推进 |
